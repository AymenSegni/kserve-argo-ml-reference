---
# ArgoCD Application: Generative Inference
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ml-generative
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: kserve-gitops-blueprint
    app.kubernetes.io/component: inference-generative
spec:
  project: kserve-gitops-blueprint
  source:
    repoURL: 'https://github.com/AymenSegni/kserve-gitops-blueprint.git'
    targetRevision: main
    path: kserve/generative
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: ml-generative
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
